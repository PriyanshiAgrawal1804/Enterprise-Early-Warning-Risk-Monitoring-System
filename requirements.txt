pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1

# Optional (used as the reference implementation for the boosted-tree
# model; GradientBoostingClassifier from scikit-learn is used as the
# offline-friendly default in 05_predictive_model.py — swap in XGBoost
# by installing it and following the note at the top of that script):
# xgboost>=2.0
